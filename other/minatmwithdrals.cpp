// int target= Sum(Arr) - X
// search for a subarray with sum==target
//count the length of subarray
//use 2 pointer approac. if sum is lesser incrementthe right pointer, if sum is more increment the left
// finall ylength = right - left +1
// keep track of max length
// answer will be total no. of elements - maxlength
